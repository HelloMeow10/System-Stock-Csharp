@page "/"
@inject NavigationManager Nav

<div class="muted">Redirigiendo al dashboard…</div>

@code {
	protected override void OnAfterRender(bool firstRender)
	{
		if (firstRender)
		{
			Nav.NavigateTo("/dashboard", replace: true);
		}
	}
}
